# FAIR Monte Carlo Simulator - Environment Variables
# 
# INSTRUCTIONS:
# 1. Copy this file: cp .env.example .env
# 2. Update the values below with your actual credentials
# 3. NEVER commit .env to Git (it's in .gitignore)
#
# For production deployment, set these as server environment variables

# Database Configuration
DB_HOST=localhost
DB_NAME=fair_monte_carlo
DB_USER=root
DB_PASS=your_password_here

# Application Settings
APP_ENV=development
APP_DEBUG=true

# Security (generate secure random strings for production)
APP_KEY=your_secret_key_here

# Server Configuration
MAX_EXECUTION_TIME=300
MEMORY_LIMIT=512M
